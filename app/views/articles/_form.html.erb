<%= tinymce_assets %>
<%= tinymce %>

  <div class="my-2 my-md-0 mr-md-3">
    <%= link_to "答案", new_article_path(answer: true), class:"btn btn-sm", style:"background-color: #33404D; color: white;"%>
    <%= link_to "記事", new_article_path, class:"btn btn-outline btn-sm" %>
  </div>

<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <% if params[:answer] %>
    <div>
      <%= form.label :year, style: "display: block" %>
      <%= form.number_field :year %>
    </div>
    <div>
      <%= form.label :comment, style: "display: block" %>
      <%= form.text_field :comment %>
    </div>
    <div>
      <%= form.hidden_field :answer, :value => true %>
    </div>
  <% end %>

  <div>
    <%= form.text_area :body, :class => "tinymce"%>
  </div>

  <div>
    <%= form.submit '下書き保存', :status => 'draft', class: "btn btn-primary" %>
    <%= form.submit '公開', :status => 'published', class: "btn btn-primary" %>
  </div>
<% end %>
