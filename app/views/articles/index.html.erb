<p style="color: green"><%= notice %></p>
  <%= week_calendar(events: @articles, number_of_weeks: 2) do |date, articles| %>
    <div class="day">
      <%= date.day %>
    <% articles.each do |article| %>
      <div class="card-header">
        <h5 class="card-title">
          <%= link_to article.title, article %>
        </h5>
      </div>
      <div class="card-body">
        <p class="card-text">
          <%= truncate(article.body, length: 10, omission: '...') %>
        </p>
      </div>
      <div class="card-footer">
        <p class="card-text">
          From: <%= article.start_time.strftime('%A, %B %d, %Y at %I:%M %p') %>
        </p>
        <p class="card-text">
          To: <%= article.end_time.strftime('%A, %B %d, %Y at %I:%M %p') %>
        </p>
      </div>
    <% end %>
  <% end %>
  <div class="row">
    <div class="col-sm-2 text-center">
      <p>次会う人</p>
      <hr>
      <div id="users">
        <% @users.each do |user| %>
          <%= render user %>
        <% end %>
      </div>
    </div>
    <div class="col-sm-8">
      <div id="articles">
        <% @articles.each do |article| %>
          <%= render article %>
        <% end %>
      </div>
      <%= paginate @articles %>
      <% if Rails.env == "development" %>
        <p>
          debug用に記事数を表示:<%= @number_of_articles %>
        </p>
      <% end %>
    </div>
  <% if false %>
    TODO: カテゴリ一覧は、とりあえず表示しない。
    <div class="col-sm-2">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">カテゴリ一覧</h5>
          <p class="card-text">
            <% @categories.each do |category| %>
              <%= category.name %>
            <% end %>
          </p>
        </div>
      </div>
    </div>
  <% end %>
  </div>
